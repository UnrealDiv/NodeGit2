const sizes = {
  width:window.innerWidth,
  height:window.innerHeight
}

function setup() {
  createCanvas(sizes.width, sizes.innerHeight);
}

function draw() {
  background(0);
  fill(255);
  ellipse(10,10,10);
}
